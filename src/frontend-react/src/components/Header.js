import React from 'react';
import { Typography } from '@mui/material';

function Header() {
  return (
    <header style={{ textAlign: 'center', margin: '30px 0' }}>
      <Typography variant="h4">
        Check Your Domain Legitimacy
      </Typography>
    </header>
  );
}

export default Header;
